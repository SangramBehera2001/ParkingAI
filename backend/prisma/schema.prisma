// 🔹 Modified by Dibyaranjan Swain
// Purpose: Upgrade existing schema to support QR + masking + call routing system

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id        Int       @id @default(autoincrement())
  name      String
  email     String    @unique
<<<<<<< Updated upstream
  phone     String    @unique
=======
  phone     String    @unique   // 🔥 Real phone (will be encrypted at service layer)

>>>>>>> Stashed changes
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  vehicles  Vehicle[]

  // 🔹 Added by Dibyaranjan Swain
  // Purpose: Track all tokens (QRs) generated for this user
  tokens    Token[]

  @@map("users")
}

model Vehicle {
  id            Int      @id @default(autoincrement())
  vehicleNumber String   @unique
  userId        Int

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  user   User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  tokens Token[]

  @@map("vehicles")
}

<<<<<<< Updated upstream
model Token {
  id          Int      @id @default(autoincrement())
  publicToken String   @unique
  privateToken String   @unique   // ADD THIS FOR privateToken
  isActive    Boolean  @default(true)
  vehicleId   Int
  qrCodeUrl   String?  @db.LongText   // Krishna ADD THIS LINE
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt


  vehicle Vehicle @relation(fields: [vehicleId], references: [id], onDelete: Cascade)
=======
model ProxyNumber {
  id        Int     @id @default(autoincrement())

  // 🔹 Added by Dibyaranjan Swain
  // Purpose: Virtual number (masked number shown in QR)
  number    String  @unique

  // 🔹 Added by Dibyaranjan Swain
  // Purpose: Enable/disable number dynamically
  isActive  Boolean @default(true)

  // 🔹 Added by Dibyaranjan Swain
  // Purpose: Prevent same number being used in multiple active calls
  isBusy    Boolean @default(false)

  // 🔹 Added by Dibyaranjan Swain
  // Purpose: Relation with tokens using this proxy
  tokens    Token[]

  @@map("proxy_numbers")
}

model Token {
  id           Int      @id @default(autoincrement())

  publicToken  String   @unique
  privateToken String   @unique   // 🔹 Used internally for secure validation

  isActive     Boolean  @default(true)

  vehicleId    Int

  // 🔹 Added by Dibyaranjan Swain
  // Purpose: Link token directly to user (faster lookup during call routing)
  userId       Int

  // 🔹 Added by Dibyaranjan Swain
  // Purpose: Assign proxy (masked number)
  proxyNumberId Int

  qrCodeUrl    String?  @db.LongText

  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  vehicle      Vehicle      @relation(fields: [vehicleId], references: [id], onDelete: Cascade)

  // 🔹 Added by Dibyaranjan Swain
  user         User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  // 🔹 Added by Dibyaranjan Swain
  proxyNumber  ProxyNumber  @relation(fields: [proxyNumberId], references: [id])

  // 🔹 Added by Dibyaranjan Swain
  // Purpose: Track all calls made using this token
  callLogs     CallLog[]
>>>>>>> Stashed changes

  @@map("tokens")
}

<<<<<<< Updated upstream
=======
model CallLog {
  id          Int      @id @default(autoincrement())

  // 🔹 Added by Dibyaranjan Swain
  // Purpose: Caller number (person who scanned QR)
  caller      String

  // 🔹 Added by Dibyaranjan Swain
  // Purpose: Actual receiver (real user phone)
  receiver    String

  // 🔹 Added by Dibyaranjan Swain
  // Purpose: Proxy number used in call
  proxyNumber String

  tokenId     Int

  createdAt   DateTime @default(now())

  // 🔹 Added by Dibyaranjan Swain
  token       Token    @relation(fields: [tokenId], references: [id])

  @@map("call_logs")
}
>>>>>>> Stashed changes
